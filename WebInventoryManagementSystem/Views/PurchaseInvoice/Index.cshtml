@model IEnumerable<WebInventoryManagementSystem.purchaseInvoice>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_sidePanelPage.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.pi_date)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.supplier.sup_name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.pi_date)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.supplier.sup_name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.pi_id }) |
            @Html.ActionLink("Details", "Details", new { id=item.pi_id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.pi_id })
        </td>
    </tr>
}

</table>
